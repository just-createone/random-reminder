# 随机提醒器 MVP 验收清单

本文档用于判断随机提醒器是否达到个人可用版本和公开测试版本的基本要求。

当前版本：`v0.1.3`

---

## 一、提醒内容管理

- [x] 可以创建提醒内容
- [x] 创建成功后页面立即显示
- [x] 刷新页面后提醒仍然存在
- [x] 可以删除提醒
- [x] 删除前出现确认提示
- [x] 删除成功后页面立即更新
- [x] 长文本不会破坏页面布局
- [ ] 没有提醒时显示明确的空状态提示

验收结果：

```text
提醒内容创建、显示、持久化和删除流程已经通过。
空状态显示仍需单独验证。
```

---

## 二、提醒设置

- [x] 可以打开提醒总开关
- [x] 可以关闭提醒总开关
- [x] 关闭总开关后执行器暂停发送提醒
- [x] 重新打开后执行器恢复工作
- [x] 可以选择全天提醒
- [x] 可以设置开始时间
- [x] 可以设置结束时间
- [x] 设置保存后出现成功提示
- [x] 刷新页面后设置仍然存在
- [ ] 非法时间范围能够被拦截或提示

验收结果：

```text
提醒总开关、全天模式、时间范围和设置持久化已经通过。
非法时间范围仍需进行异常输入测试。
```

---

## 三、今日计划

- [x] 可以生成今日提醒计划
- [x] 可以强制重新生成今日计划
- [x] 只生成当前时间之后的提醒
- [x] 已发送记录不会被重新生成覆盖
- [x] 失败记录能够保留
- [x] 过期提醒能够被跳过
- [x] 下一次提醒时间显示正确
- [x] 倒计时能够自动更新
- [x] 提醒触发后页面能够显示新的下一次提醒

验收结果：

```text
今日计划生成、重新生成、状态保留、过期处理和倒计时更新已经通过。
```

---

## 四、通知功能

### Windows 本地通知

- [x] 测试通知接口可以正常调用
- [x] Windows 系统通知能够显示
- [x] 本地通知失败时能够回退到控制台
- [x] 通知失败不会导致执行器退出

### 浏览器通知与 Web Push

- [x] 浏览器能够请求通知权限
- [x] 用户允许后权限状态可以变为 `granted`
- [x] Web Push 订阅能够保存
- [x] Service Worker 能够处理推送事件
- [x] 后端能够调用 Web Push 发送流程
- [x] 推送通知标题和正文可以正常生成
- [ ] 禁止通知后页面能够给出明确提示
- [ ] 真实手机后台能够收到 Web Push
- [ ] 真实手机锁屏后能够收到 Web Push
- [ ] 点击手机通知后能够打开正确页面

验收结果：

```text
Windows 本地通知和 Web Push 技术链路已经完成。
真实手机后台、锁屏和点击跳转仍需验收。
```

---

## 五、PWA

- [x] Manifest 可以正常加载
- [x] Service Worker 可以正常注册
- [x] 192×192 图标显示正常
- [x] 512×512 图标显示正常
- [x] 浏览器出现安装入口
- [x] 可以安装到电脑桌面
- [ ] 安装后独立窗口完整验收通过
- [ ] PWA 首页能够正常加载并完成核心操作
- [ ] PWA 关闭后可以通过快捷方式重新打开
- [ ] PWA 从后台恢复后页面状态正常
- [ ] 页面刷新后不会出现空白页
- [ ] 更新 Service Worker 后可以获得新版本
- [ ] 真实手机可以安装 PWA
- [ ] 真实手机 PWA 独立窗口运行正常

验收结果：

```text
PWA 安装能力已经通过。
独立窗口、后台恢复、更新流程和真实手机安装仍需验收。
```

---

## 六、数据库

- [x] 应用首次启动时能够自动创建数据库
- [x] 提醒内容能够持久化
- [x] 设置能够持久化
- [x] 今日计划能够持久化
- [x] 通知记录能够持久化
- [x] 容器重启后数据仍然存在
- [ ] 执行 `docker compose down` 后数据仍然存在
- [x] 数据库文件没有被 Git 跟踪

验收结果：

```text
数据库初始化和核心数据持久化已经通过。
完整执行 docker compose down 后的数据保留仍需单独复测。
```

---

## 七、数据库备份

- [x] 本地备份命令可以执行
- [x] 容器内备份命令可以执行
- [x] 备份文件可以生成到 `backups/`
- [x] 备份文件能够通过 SQLite 完整性检查
- [x] 备份过程不会锁死正式数据库
- [x] 备份文件不会被 Git 跟踪
- [x] 每次备份后能够执行旧备份清理

验收结果：

```text
数据库备份流程已经完成并通过自动化测试。
```

---

## 八、数据库恢复

- [x] 恢复命令可以读取有效备份
- [x] 损坏的备份会被拒绝
- [x] 恢复前能够创建安全备份
- [x] 恢复后数据库完整性检查通过
- [x] 恢复后的提醒数据正确
- [x] 数据库被占用时显示明确错误
- [x] 停止应用后可以完成正式恢复
- [x] 恢复失败不会破坏原数据库

验收结果：

```text
数据库恢复、安全备份和完整性检查已经通过。
```

---

## 九、备份保留策略

- [x] 可以设置保留最新备份数量
- [x] 可以设置最大保存天数
- [x] `--dry-run` 不会实际删除文件
- [x] 正式清理只删除符合规则的旧备份
- [x] 最新保留范围内的备份不会被删除
- [x] 非标准命名文件不会被误删
- [x] 清理结果会写入日志

当前正式策略：

```text
保留最新 30 份备份
最大保存时间 90 天
```

验收结果：

```text
备份保留、预览和正式清理流程已经通过。
```

---

## 十、本地运行

- [x] 可以创建 Python 虚拟环境
- [x] 可以安装 `requirements.txt`
- [x] 可以通过 `python -m backend.run` 启动
- [x] 首页可以访问
- [x] API 文档可以访问
- [x] 健康接口可以访问
- [x] 停止应用时执行器能够正常关闭

验收结果：

```text
本地开发和运行流程已经通过。
```

---

## 十一、Docker 部署

- [x] 本地镜像能够构建
- [x] Docker Compose 配置可以解析
- [x] 容器能够启动
- [x] 容器能够变为 `healthy`
- [x] 宿主机端口映射正确
- [x] 数据目录挂载正确
- [x] 备份目录挂载正确
- [x] VAPID 密钥目录只读挂载
- [x] 正式镜像可以从 GHCR 拉取
- [x] ARM64 镜像能够运行
- [x] AMD64 镜像能够构建

验收结果：

```text
本地 Docker、正式 Compose、ARM64 运行和多架构镜像构建已经通过。
```

---

## 十二、全新安装

- [x] 可以从空目录克隆仓库
- [x] 可以从示例文件创建 `.env.release`
- [x] 可以只依靠部署文档完成启动
- [x] 全新数据库能够自动初始化
- [x] 新用户可以创建第一条提醒
- [x] 重启容器后数据仍然存在
- [x] 全新环境可以创建数据库备份
- [x] 日志中没有启动异常

测试环境：

```text
D:\Project\随机提醒器\fresh-install-20260802-124728
```

测试端口：

```text
8001
```

验收结果：

```text
全新安装演练已经通过。
```

---

## 十三、自动化测试与持续集成

- [x] Windows 测试通过
- [x] Ubuntu 测试通过
- [x] 全部 Pytest 测试通过
- [x] Python 语法编译检查通过
- [x] Docker AMD64 构建通过
- [x] Docker ARM64 构建通过
- [x] Git 标签能够触发镜像发布
- [x] GHCR 能够生成版本标签
- [x] GHCR 能够更新 `latest`

验收结果：

```text
自动化测试、GitHub Actions 和多架构镜像发布流程已经通过。
```

---

## 十四、稳定性测试

### 一小时快速测试

- [x] 容器持续运行一小时
- [x] 所有健康检查成功
- [x] 容器状态始终为 `running`
- [x] 健康状态始终为 `healthy`
- [x] 容器重启次数为 `0`
- [x] 页面持续可以访问
- [x] 提醒数据没有丢失
- [x] 日志中没有 `Traceback`

#### 一小时稳定性测试结果

测试日期：2026-08-02  
应用版本：`v0.1.3`  
测试环境：全新安装 Docker 环境  
宿主机端口：`8001`  
监控间隔：5 分钟  
监控时长：1 小时  
监控次数：12 次

测试结果：

- 容器始终保持 `running`
- 健康状态始终为 `healthy`
- 健康接口始终返回 `ok`
- 容器重启次数始终为 `0`
- 提醒数据没有丢失
- 内存使用基本稳定在约 `62 MiB`
- CPU 大部分时间低于 `1%`
- 出现一次短时 CPU 波动，之后恢复正常
- 没有发现 `ERROR`
- 没有发现 `Traceback`
- 没有发现数据库异常

测试结论：

```text
一小时快速稳定性测试通过
```

### 二十四小时测试

当前状态：

```text
正在进行
```

待验证项目：

- [ ] 容器持续运行二十四小时
- [ ] 健康接口没有失败
- [ ] 容器重启次数为 `0`
- [ ] 至少一条计划提醒正常触发
- [ ] 通知能够正常发送
- [ ] 今日计划能够正常更新
- [ ] 数据库没有锁定异常
- [ ] 内存使用没有持续异常增长
- [ ] 日志中没有严重错误

测试要求：

- 电脑保持接通电源
- 电脑不进入睡眠或休眠
- Docker Desktop 保持运行
- 监控终端保持打开
- 测试容器不停止、不重启
- 测试期间不修改测试环境业务代码

---

## 十五、真实手机基础验收

测试方式：

```text
手机和电脑连接同一局域网
手机通过电脑 IPv4 地址和 8001 端口访问应用
```

检查项目：

- [x] 手机可以通过局域网访问应用
- [x] 手机端首页显示正常
- [x] 手机端提醒管理页面显示正常
- [x] 手机端设置页面显示正常
- [x] 手机端可以创建提醒
- [x] 手机端可以删除提醒
- [x] 手机端可以保存设置
- [x] 刷新后提醒数据仍然存在
- [x] 刷新后设置数据仍然存在
- [x] 页面没有明显横向滚动
- [x] 按钮和输入框没有被截断
- [x] 删除确认框能够正常操作
- [x] 手机触摸操作正常
- [ ] 手机横屏模式通过
- [ ] 系统字体放大模式通过
- [ ] 不同手机和浏览器兼容性通过
- [ ] 真实手机 PWA 安装通过
- [ ] 真实手机后台通知通过
- [ ] 真实手机锁屏通知通过

验收结果：

```text
真实手机浏览器基础页面和核心操作验收通过。
PWA 安装和真实通知触达仍需后续验证。
```

---

## 十六、文档

- [x] README 内容与当前版本一致
- [x] 本地启动命令正确
- [x] Docker 部署命令正确
- [x] 环境变量说明完整
- [x] 部署指南可以使用
- [x] 回滚流程已经记录
- [x] 数据库恢复流程已经记录
- [x] 故障排查文档已经完成
- [x] MVP 验收文档已经建立
- [x] 产品路线文档已经建立
- [x] 界面验收文档已经建立
- [x] 首批用户测试计划已经建立
- [x] 版本发布检查清单已经建立
- [x] 版本变更记录已经建立
- [x] 开发与贡献规范已经建立
- [x] 文档中没有真实邮箱
- [x] 文档中没有私钥内容

验收结果：

```text
MVP 阶段主要开发、部署、测试和发布文档已经完成。
```

---

## 十七、安全检查

- [x] `.env.release` 没有被 Git 跟踪
- [x] 数据库文件没有被 Git 跟踪
- [x] 数据库备份没有被 Git 跟踪
- [x] VAPID 私钥没有被 Git 跟踪
- [x] 日志文件没有被 Git 跟踪
- [ ] Git 完整历史中没有真实私钥
- [x] 示例配置使用通用邮箱
- [x] 正式密钥只保存在本地
- [x] 文档敏感信息搜索没有发现结果

待完成：

```text
发布最终版本前，对完整 Git 历史执行一次敏感信息检查。
```

---

## 十八、MVP 完成标准

以下条件全部满足后，可以将项目认定为个人可用 MVP：

- [x] 核心提醒功能通过
- [x] Windows 本地通知通过
- [x] 浏览器通知和 Web Push 技术链路通过
- [x] PWA 安装通过
- [x] 数据库持久化通过
- [x] 备份、恢复和清理通过
- [x] 正式 Docker 部署通过
- [x] 全新安装演练通过
- [x] 一小时稳定性测试通过
- [ ] 二十四小时稳定性测试通过
- [x] 自动化测试全部通过
- [x] 真实手机基础验收通过
- [ ] PWA 独立窗口完整验收通过
- [ ] 真实手机通知触达通过
- [ ] 最终文档和安全检查通过

当前结论：

```text
项目已经达到接近个人可用 MVP 的状态。

正式完成 MVP 前仍需完成：
1. 二十四小时稳定性测试
2. PWA 独立窗口验收
3. 真实手机通知触达测试
4. Git 完整历史敏感信息检查
5. 最终验收复测
```

---

## 十九、当前验收状态

| 验收项目 | 状态 |
|---|---|
| 核心提醒功能 | 已完成 |
| Windows 本地通知 | 已完成 |
| 浏览器通知与 Web Push | 技术链路已完成 |
| PWA 安装 | 已完成 |
| PWA 独立窗口验收 | 未完成 |
| 真实手机基础验收 | 已通过 |
| 真实手机通知验收 | 未完成 |
| 数据库持久化 | 已完成 |
| 数据库备份 | 已完成 |
| 数据库恢复 | 已完成 |
| 备份自动清理 | 已完成 |
| Docker 部署 | 已完成 |
| 正式镜像发布 | 已完成 |
| 全新安装演练 | 已通过 |
| 一小时稳定性测试 | 已通过 |
| 二十四小时稳定性测试 | 进行中 |
| 界面基础验收 | 已通过 |
| 自动化测试 | 已通过 |
| 文档整理 | 已完成，持续维护 |
| 最终 MVP 验收 | 未完成 |

---

## 二十、剩余验收顺序

按照以下顺序完成剩余工作：

```text
完成二十四小时稳定性测试
→ 完成 PWA 独立窗口验收
→ 完成 PWA 后台恢复验收
→ 完成真实手机通知触达测试
→ 检查 Git 完整历史中的敏感信息
→ 执行最终 MVP 回归测试
→ 更新验收状态
→ 确认 v0.1.3 MVP 完成
```